import SwiftUI

  struct ContentView: View {
      var body: some View {
          VStack {
              Text("CalTrackPro")
                  .font(.largeTitle)
                  .padding()

              Text("App is working!")
                  .padding()
          }
      }
  }

  #Preview {
      ContentView()
  }
